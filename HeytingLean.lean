import HeytingLean.Tests.Integration.OrganicAlignmentStack

/-!
Package root for the `HeytingLean` library in the Futamura–Adelic (Organic Alignment) bundle.

This file exists so `lake build` succeeds without requiring users to specify targets.
The primary compile-time integration hook is:

- `HeytingLean.Tests.Integration.OrganicAlignmentStack`
-/

